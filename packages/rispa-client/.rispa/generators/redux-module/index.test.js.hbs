import { Effects, getModel, getEffect } from 'redux-loop'

import {{ camelCase name }}, {

} from './index.js'

describe('@reducer {{ camelCase name }}', () => {
  it('should have initialState', () => {
    expect({{ camelCase name }}()).to.not.equal(undefined)
  })

  it('should do thing', () => {

  })
})